# Copy to .env.local and fill in. Both values are public-safe (the anon key is
# designed to ship in the client bundle). Real secrets live server-side in Supabase.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
